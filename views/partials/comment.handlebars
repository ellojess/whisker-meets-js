<div class="col-xs-12 comment-indent">
    <p>{{comment.content}}</p>
    <p class="text-right">{{comment.author.username}}</p>
    <a href="/dogs/{{dogId}}/comments/{{comment._id}}/replies/new">Reply</a>
    <br>
    {{#each comment.comments}}
        {{> comment comment=this dogId=../dogId}}
    {{/each}}
</div>